{
  "name": "convolver-player-monorepo",
  "private": true,
  "workspaces": [
    "packages/vue/*",
    "packages/core/*"
  ],
  "scripts": {
    "dev": "yarn workspace @convolver-player/vue dev",
    "build": "yarn workspaces foreach run build",
    "test": "yarn workspaces foreach run test",
    "test:coverage": "yarn workspaces foreach run test:coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.0.9",
    "jsdom": "^27.2.0",
    "react": "^18.2.0",
    "vite": "^5.0.0",
    "vitest": "^4.0.9",
    "vue": "^3.3.0"
  }
}
