{
  "name": "convolver-player-monorepo",
  "license": "MIT",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/vue/*",
    "packages/core/*",
    "packages/react/*"
  ],
  "scripts": {
    "dev": "yarn workspaces run dev",
    "build": "yarn workspaces run build",
    "test": "yarn workspaces run test:run",
    "test:coverage": "yarn workspaces run test:run:coverage",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "yarn build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "@vitest/coverage-v8": "^4.0.9",
    "jsdom": "^27.2.0",
    "react": "^18.2.0",
    "vite": "npm:rolldown-vite@7.2.2",
    "vitest": "latest",
    "vue": "^3.3.0",
    "react-dom": "^18.2.0"
  }
}